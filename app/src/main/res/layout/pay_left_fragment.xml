<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent"

        >

    <TextView
            android:id="@+id/text_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/content_title_height"
            android:paddingLeft="@dimen/fragment_food_left_item_padding"
            android:text="付款方式："
            android:textSize="@dimen/pay_title_text_size"
            android:textColor="#ffffff"
            />
    <include
            android:id="@+id/layout_zhifubao"
            layout="@layout/pay_left_item"
            />

    <include
            android:id="@+id/layout_wechat"
            layout="@layout/pay_left_item"
            />


</LinearLayout>
